#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "functions.h"

int main() {

    int numa=0;
    int numb=0;
  scanf("%d",&numa);
  scanf("%d",&numb);
  placas(numa,numb);

  return(0);
}